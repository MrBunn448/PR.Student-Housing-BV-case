Dependencies

node_modules/              # Exclude all project dependencies
node_modules
Build Artifacts

/client/dist               # Exclude frontend build files (Vite/React output)
/server/dist               # Exclude backend build files (if compiled)

Credentials & Runtime

.env                       # Exclude environment configuration files
.env.local                 # Exclude local environment overrides

Critical: Exclude files that hold local MySQL passwords

Git is already tracking these, but keeping the rule for safety

/server/server.js
/server/setup_database.js

Operating System / IDE / Logs

.DS_Store                  # macOS system files
.log                      # Generated logs
npm-debug.log # Debug logs generated by npm
yarn.lock                  # Lock file for Yarn dependency manager