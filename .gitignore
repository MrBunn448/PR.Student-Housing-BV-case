Dependencies

node_modules/ # Exclude all project dependencies
Build Artifacts

/client/dist # Exclude frontend build files (Vite/React output) /server/dist # Exclude backend build files (if compiled)
Credentials & Runtime

.env # Exclude environment configuration files .env.local # Exclude local environment overrides
Critical: Exclude files that hold local MySQL passwords

/server/server.js # Excludes connection file with local credentials /server/setup_database.js # Excludes migration script with local credentials
Tooling & Logs

.DS_Store # macOS system files .log # Generated logs yarn.lock # Lock file for Yarn dependency manager npm-debug.log # Debug logs generated by npm